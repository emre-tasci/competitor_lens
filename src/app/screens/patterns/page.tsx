import { PatternExplorer } from "@/components/screens/PatternExplorer";

export const metadata = { title: "Desenler · Product Terminali" };

export default function PatternsPage() {
  return <PatternExplorer />;
}
